beforeEach(``, async({ page }){ //Before each can have page

});

beforeAll(``, async({ browser }){ //Before all can have browser only
    const context = await browser.newContext()
    const page = await context.newPage();
});

imports and Class

when class is exported like export {ClassName}; then import like import {ClassName} from '';
when class is exported like export default new ClassName(); or export ClassName {body} then import like import ClassName from '' -->Singleton

with "type": "module" always use @param {import("@playwright/test").Page} page in-line now with global import

